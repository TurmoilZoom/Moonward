<?xml version="1.0" encoding="utf-8" ?>
<sf:WindowEx x:Class="Starward.Features.Update.UpdateTipWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sf="using:Starward.Frameworks"
             Title="UpdateTipWindow"
             x:DefaultBindMode="OneWay"
             mc:Ignorable="d">

    <!--  窗口本体是透明的（TransparentBackdrop），看得见的只有下面这张卡片；
          窗口尺寸按卡片实测尺寸设置，故卡片左上对齐、正好铺满整窗。  -->
    <Grid x:Name="RootGrid"
          PointerEntered="RootGrid_PointerEntered"
          PointerExited="RootGrid_PointerExited"
          PointerPressed="RootGrid_PointerPressed">
        <Border x:Name="CardBorder"
                MaxWidth="340"
                Padding="14,12"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <!--  成功 / 失败两个图标叠放、靠 Visibility 互斥：颜色一律 ThemeResource，
                      才能跟随每次显示时按系统深浅色设置的 RootGrid.RequestedTheme。  -->
                <Grid VerticalAlignment="Center">
                    <FontIcon FontSize="20"
                              Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                              Glyph="&#xE930;"
                              Visibility="{x:Bind ToVisibilityReversed(IsError)}" />
                    <FontIcon FontSize="20"
                              Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                              Glyph="&#xE783;"
                              Visibility="{x:Bind ToVisibility(IsError)}" />
                </Grid>
                <StackPanel Grid.Column="1" Spacing="2">
                    <TextBlock MaxLines="3"
                               Text="{x:Bind TipTitle}"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="Wrap" />
                    <TextBlock FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Text="{x:Bind TipDetail}"
                               TextWrapping="Wrap"
                               Visibility="{x:Bind ToVisibility(HasDetail)}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>

</sf:WindowEx>
